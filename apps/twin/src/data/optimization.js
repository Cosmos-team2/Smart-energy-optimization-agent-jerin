// This file's real content lives at packages/shared/twin-data/optimization.js
// — it's shared with apps/hero (the marketing hero's story sequence imports
// the same baseline/optimized peak values and timeline). Re-exported here
// unchanged so this app keeps an obvious `src/data/` folder and every
// existing import of "./data/optimization.js" inside this app keeps working
// without modification. See that file for the full MILP-trace comments.
export * from "../../../../packages/shared/twin-data/optimization.js";
